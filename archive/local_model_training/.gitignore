# =============================================================================
# 凭据 —— 绝不提交
# =============================================================================
# 本工程只从环境变量 DEEPSEEK_API_KEY 读取密钥，不存在任何凭据文件，
# 但下面这些模式仍然保留，防止有人误把密钥写进 .env 之后提交。
.env
.env.*
!.env.example
*.key
*.pem
secrets.yaml
secrets.yml
credentials.json
service-account.json

# =============================================================================
# 生成的数据与断点
# =============================================================================
data/raw/
data/splits/
data/sft/
data/fixtures/
data/reports/
*.checkpoint.json
.generation_state/

# 注意：data/raw/.checkpoint.json 是生成器的断点文件，可能来自正在进行的
# 生成任务（多个工作区/进程共用同一份数据时尤其如此）。清理临时文件时
# 不要顺手删掉它 —— 删了会让生成进度丢失。

# =============================================================================
# 训练与报告产物
# =============================================================================
outputs/
runs/
reports/
wandb/
checkpoints/
*.safetensors
*.gguf
*.bin
*.pt
*.ckpt

# =============================================================================
# Python
# =============================================================================
__pycache__/
*.py[cod]
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.venv/
venv/
env/

# =============================================================================
# 工具
# =============================================================================
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml
junit*.xml

# =============================================================================
# 编辑器 / 系统
# =============================================================================
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# =============================================================================
# 本地临时工作目录（脚本自检用）
# =============================================================================
work_test/
work/
tmp/
