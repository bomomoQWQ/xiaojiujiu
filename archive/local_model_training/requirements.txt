# =============================================================================
# 核心依赖：数据生成 / 校验 / 切分 / 评测 / 基准 全部只需这些
# =============================================================================
# 安装：pip install -r requirements.txt
#
# 只装这些即可运行 `qboss gen / validate / split / sft --no-tokenize / eval
# --backend echo / bench --backend echo`，以及**全部离线测试**（pytest）。
# 也就是说：不装 torch、不下模型、不联网，也能验证整条数据管线。
# =============================================================================

PyYAML>=6.0
jsonschema>=4.18
httpx>=0.24

# 测试（离线，无需大模型与网络）
pytest>=8.0
