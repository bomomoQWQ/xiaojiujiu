---
title: "架构补丁 v0.2：即时演出层与持久认知层分离，移除本地 2B 核心依赖"
patch_id: "PATCH-RUNTIME-ACTING-PERSISTENCE-SEPARATION"
version: "v0.2"
supersedes: "PATCH-CPU-2B-REMOVAL v0.1"
applies_to: "内源主动型长期陪伴 AI Runtime：完整认知架构与协议设计"
status: "建议替换上一版补丁"
---

# 架构补丁 v0.2  
## 即时演出层与持久认知层分离，移除本地 2B 核心依赖

---

# 0. 补丁摘要

本补丁修正原架构对“本地 2B 情绪模型”的依赖，并进一步明确一个比“2B 太慢”更重要的认知：

> **当前这一轮的即时理解和即时情绪表现，本来就应该由主 LLM 完成。**

因为主 LLM 在当前轮能够直接看到：

```text
宿主角色设定
+
当前对话上下文
+
用户刚刚说的话
+
Runtime 已有的持久状态
```

因此，Runtime 没有必要为了让主 LLM“理解当前这句话”，先同步调用一个 2B 模型做：

```text
用户消息
→ 事件评价
→ 情绪数值
→ 心理解释
→ 主 LLM
```

这种路径既昂贵，又重复了主 LLM 已经具备的即时语义理解能力。

新的架构原则是：

> **主 LLM 负责“现在这一刻怎么反应”；Runtime 负责“这一刻过去以后留下些什么”。**

因此，本补丁正式将系统拆成两个时间尺度：

```text
【即时演出层】
当前用户消息
+ 当前对话上下文
+ 宿主人格
+ 已有 Runtime 状态
↓
主 LLM
↓
本轮即时反应

【持久认知层】
本轮交互沉淀
↓
记忆 / 情绪余波 / 用户模型 / 未尽之事
↓
主动动力 / 候选意图 / 后验重解释
↓
影响未来轮次
```

本地 2B 生成式模型不再是标准依赖。

---

# 1. 为什么要重写上一版补丁

上一版补丁的主要结论是：

> 本地 2B 太慢，因此应该从同步关键路径中移除，并降级为异步语义增强器。

这个结论仍然正确。

但进一步分析后发现：

> **问题不只是“2B 太慢”，而是“这部分工作本来就不该堵在主 LLM 前面”。**

即使本地 2B 足够快，也没有必要让它成为：

```text
当前用户消息
→ 情绪解释
→ 主 LLM
```

的必经步骤。

因为主 LLM 本身已经能直接理解：

```text
“算了，也没什么。”
```

并结合完整当前上下文作出即时反应。

Runtime 真正独特的价值不在于：

> 再做一遍当前轮语义理解。

而在于：

> **把当前发生的事情转化为跨轮、跨小时、跨天持续存在的内部状态。**

---

# 2. 实测仍然构成硬约束

本补丁继续保留 CPU 实测结论。

测试环境：

```text
CPU：
Intel Core i7-13700H

使用：
8 个性能核

模型规模：
约 2B

量化：
Q4_K_M
```

实测：

```text
生成速度：
≈ 18 token/s

单次评价输出：
≈ 67 token

纯生成：
≈ 3.7 s
```

提示处理：

```text
输入长度：
≈ 250～600 token

处理速度：
≈ 157 token/s

提示处理：
≈ 1.6～3.8 s
```

因此单次评价理论总耗时：

```text
≈ 5.3～7.5 s
```

实测：

```text
p50 ≈ 6.65 s
```

仅 250 token 输入处理：

\[
250 / 157 \approx 1.59s
\]

已经超过原本约 1.2 秒的同步 Runtime 前处理预算。

因此：

> **纯 CPU 2B 不适合作为任何实时同步依赖。**

但本补丁进一步指出：

> **即便它足够快，也不应该成为当前轮即时演出的语义前置条件。**

---

# 3. 新的双层时间模型

系统正式拆成：

```text
即时演出层
+
持久认知层
```

两层职责必须严格分开。

---

# 4. 即时演出层

即时演出层负责：

> **“用户现在说了这句话，我这一刻怎么反应？”**

由宿主主 LLM完成。

输入：

```text
宿主角色设定
+
最近对话上下文
+
当前用户原话
+
当前确定事实
+
已有 Runtime 持久状态
+
必要的心理状态缓存
```

输出：

> 当前轮可见回复。

---

## 4.1 主 LLM 本来就拥有即时理解能力

例如：

```text
char:
“你今晚还会回来吗？”

user:
“不知道，可能没时间。”
```

主 LLM 本身可以即时表现：

```text
舍不得
轻微失落
理解
克制
调侃
迟钝
```

具体如何表现，取决于：

- 当前上下文；
- 宿主人格；
- 已有长期心理状态；
- 用户原话。

不需要先把：

```text
负向 = 0.46
不确定性 = 0.61
```

转一遍再告诉主 LLM。

---

## 4.2 即时演出不等于持久状态写入

主 LLM 当场表现出：

> “有点舍不得。”

并不意味着 Runtime 必须立即写入：

```text
persistent_sadness = 0.7
```

主 LLM 的可见表现：

> 是当场演出。

Runtime 状态：

> 是持续认知。

两者不是同一权限。

---

# 5. 持久认知层

持久认知层负责：

> **“这件事结束以后，它在我身上留下了什么？”**

主要处理：

```text
情绪余波
背景心境
记忆
未尽之事
用户交互证据
用户模型
候选意图
主动动力
后验重解释
```

它不要求每一轮都实时语义完备。

---

## 5.1 Runtime 的核心目标从“即时理解”改为“长期连续性”

Runtime 的任务不是：

```text
每条消息都准确判断：
这是失落 0.43
焦虑 0.27
嫉妒 0.11
```

而是：

```text
这件事是否值得留下？
是否形成未尽之事？
是否改变背景心境？
是否留下用户偏好证据？
是否需要以后重新解释？
是否影响未来主动性？
```

---

# 6. 新的完整当前轮流程

用户发送：

```text
“今晚可能不来了。”
```

流程：

```text
用户消息到达
↓
lazy_tick(now)
↓
写入原始事件
↓
入口边界规则检查
↓
更新机械性工作局势
↓
读取已有 Runtime 状态
↓
主 LLM 看到当前原话和上下文
↓
立即完成本轮演出
↓
用户收到回复
```

到这里：

> **不需要任何额外生成式模型。**

之后才进入持久认知处理：

```text
本轮事件
↓
规则 / 统计 / 记忆系统
↓
能高置信结算？
├─ 是 → 更新持久状态
└─ 否 → unresolved
```

---

# 7. 当前消息与旧 Runtime 心理状态的优先级

必须正式规定：

```text
宿主最高层设定 / 安全约束
>
当前用户原话
>
当前确定事实
>
显式边界
>
Runtime 持久心理状态
>
心理解释缓存
>
主 LLM 自然发挥
```

其中最重要的是：

> **当前用户原话与当前事实高于 Runtime 旧心理缓存。**

---

## 7.1 示例

Runtime 缓存：

```text
最近整体有些失落，表达偏克制。
```

用户突然说：

```text
“其实我就是回来陪你的哈哈。”
```

主 LLM 本轮可以立刻：

```text
惊喜
放松
开心
```

不能因为旧缓存还写着“失落”而继续机械表现低落。

至于：

> 这句新话是否让持久背景心境发生改变？

由 Runtime 之后结算。

---

# 8. “即时反应”和“情绪余波”正式分离

新定义：

```text
即时情绪表现
=
主 LLM 在本轮对当前事件的自然反应
```

而：

```text
持续情绪状态
=
Runtime 对事件留下的长期影响
```

例如：

用户突然夸角色：

```text
“你今天真的很可爱。”
```

即时演出：

```text
主 LLM 当场开心 / 害羞 / 调侃
```

持久认知：

```text
本次互动形成正向影响
↓
背景心境略微上升
↓
可能形成重要记忆
↓
后续数小时仍有轻微正向余波
```

Runtime 的价值就在后半段。

---

# 9. 移除“每轮精确情绪评价”的要求

不再要求：

```text
每条用户消息
↓
必须立刻得到准确事件评价
```

改成：

```text
高置信事件
→ 粗粒度结算

低置信事件
→ unresolved

重要事件
→ 等深层语义刷新

普通低价值事件
→ 可以永远不深挖
```

---

# 10. 粗粒度持久情绪更新

Level 0 / Level 1 可以处理：

```text
明确正向
明确负向
明确拒绝
明确感谢
明确冲突
明确和解
明确边界
明显重大事件
```

产生：

```text
方向
粗粒度强度
时间
来源
```

不要求命名：

```text
嫉妒
委屈
羞耻
焦虑
```

---

## 10.1 示例

用户：

```text
“谢谢你，我今天真的被你安慰到了。”
```

Runtime 可以高置信写：

```json
{
  "direction": "+",
  "impact": "medium_high",
  "source": "explicit_positive_feedback"
}
```

而不需要生成式模型。

---

# 11. 模糊事件允许保持未解释

例如：

```text
“算了，也没什么。”
```

Runtime 可以只记录：

```json
{
  "semantic_status": "unresolved",
  "potential_relevance": "medium",
  "source_event_id": "evt_101"
}
```

主 LLM 当前轮已经有机会自然理解它。

Runtime 不必马上硬猜。

---

# 12. 延迟理解从兜底升级为正式特性

过去设计中：

> 延迟理解主要用于模型当时没分析清楚。

现在正式改为：

> **持久认知层允许有意延迟复杂语义结算。**

典型过程：

```text
用户：
“算了，也没什么。”

↓
本轮主 LLM：
可能察觉，也可能没察觉

↓
Runtime：
保存原始事件
semantic_status = unresolved

↓
几小时后用户：
“你那时候果然没发现。”

↓
深层认知刷新：
重新解释旧事件

↓
Runtime：
生成 reappraisal_event
```

例如：

```text
“现在意识到，用户当时可能确实感到失望。”
```

这可以产生：

```text
懊恼
愧疚
修复冲动
新的未尽之事
修复型候选意图
```

---

# 13. “追夫火葬场”机制因此更加自然

原先如果每轮都要求一个模型做到完美情绪理解：

> 很多误解会在发生时被提前消灭。

反而失去长期关系中很自然的一种现象：

```text
当时没理解
↓
后来出现新证据
↓
突然意识到
↓
产生新的心理反应
```

因此：

> **系统允许错过当下，但不能丢失原始证据。**

这是新架构的重要特点。

---

# 14. 心理解释不再是高频必需品

原设计：

```text
结构化情绪状态
↓
2B 情绪解释
↓
主 LLM
```

修改为：

```text
已有心理解释缓存？
├─ 有 → 可用
└─ 无 → 代码模板
```

---

## 14.1 模板只负责长期底色

例如：

```text
当前整体心境稍微偏负向。
近期存在一些尚未完全消化的负面影响。

你目前仍有一定接近用户的倾向，
但整体表现较为克制。
```

它不是为了告诉主 LLM：

> 当前这句话具体应该怎么反应。

当前反应由主 LLM自己完成。

模板只说明：

> **进入本轮之前，你大概处于什么长期状态。**

---

# 15. 深层心理解释变成缓存

当低频强语义模型被调用时，可以生成：

```text
psychological_interpretation_cache
```

例如：

> 最近的负向体验主要来自对关系连续性的轻微不确定；想主动确认用户状态，但又不希望给对方压力。

这份解释可以：

```text
跨多轮复用
```

直到：

```text
背景心境明显变化
主要活跃事件变化
重大重估事件出现
关系局势显著改变
```

才标记 stale。

即使 stale：

> 退回模板即可。

---

# 16. 本地 2B 正式从标准架构移除

原标准设计：

```text
本地 2B
├─ 事件评价
└─ 情绪解释
```

正式删除。

理由有两层：

### 工程层

纯 CPU 实测延迟太高。

### 架构层

当前轮语义理解本就由主 LLM 更自然地承担。

因此，本地 2B 不再具有足够的性价比。

---

# 17. 可选 Semantic Provider

仍保留抽象接口：

```text
SemanticProvider
```

可选实现：

```text
RemoteAPIProvider
LocalGPUProvider
LocalCPUProvider
DisabledProvider
```

但它不是 Runtime 必需依赖。

---

# 18. 强语义模型的新职责：深层认知刷新

强语义能力统一用于：

```text
复杂旧事件重解释
心理状态深层语言化
候选意图生成
复杂未尽之事识别
记忆高级整理
用户模型语义总结
```

而不是用于：

```text
每条消息的即时演出
```

---

# 19. 深层认知刷新输入

建议输入：

```text
最近积累的 unresolved 事件
+
当前工作局势
+
长期背景心境
+
活跃情绪影响事件
+
激活记忆
+
未尽之事
+
用户交互模型摘要
+
已有候选意图
+
必要的关键原文
```

---

# 20. 深层认知刷新输出

建议统一输出建议集：

```json
{
  "reinterpretations": [],
  "psychological_interpretation": {},
  "candidate_intent_operations": [],
  "memory_suggestions": [],
  "unfinished_matter_suggestions": [],
  "user_model_evidence_suggestions": []
}
```

注意：

> 仍然只有建议权。

所有修改经过：

```text
Runtime Reducer
↓
APPLY / REBASE / DISCARD
```

---

# 21. 深层认知刷新何时触发

建议只在：

```text
unresolved 事件积累到一定程度
出现重大关系事件
未尽之事进入 due
候选池不足
准备主动但语义依据不足
发现历史解释可能错误
用户出现新证据推翻旧理解
长时间没有刷新且系统空闲
```

时调用。

---

# 22. 新架构三级结构

```text
┌──────────────────────────────────────┐
│ Level 2：低频深层语义                │
│                                      │
│ 重解释 / 心理深解 / 候选意图         │
│ 记忆整理 / 用户模型语义总结          │
└─────────────────▲────────────────────┘
                  │ 非必需
┌─────────────────┴────────────────────┐
│ Level 1：廉价认知                    │
│                                      │
│ 规则 / 统计 / RAG / 全文检索 / 缓存  │
│ 用户行为基线 / 高置信事件提取         │
└─────────────────▲────────────────────┘
                  │
┌─────────────────┴────────────────────┐
│ Level 0：确定性 Runtime              │
│                                      │
│ 时间 / 状态机 / 情绪余波 / I-R-P     │
│ 边界 / 未尽之事 / 协议 / 动机决策    │
└──────────────────────────────────────┘
```

主 LLM 则位于另一条轴：

```text
当前轮即时演出
```

它不是 Level 0/1/2 中的“深层认知模块”。

---

# 23. 新总流程

```text
用户消息
↓
lazy_tick
↓
原始事件落库
↓
硬边界 / 机械字段处理
↓
读取已有 Runtime 状态
↓
主 LLM 即时演出
↓
用户看到回复
────────────────────────
之后：
↓
本轮交互沉淀
↓
规则 / 统计结算
↓
可理解部分进入持久状态
↓
不可理解部分进入 unresolved
↓
必要时低频深层认知刷新
↓
记忆 / 情绪余波 / 用户模型 / 候选意图更新
↓
影响未来轮次
```

---

# 24. 主 LLM 与 Runtime 的新权力边界

## 主 LLM 负责

```text
当前消息即时理解
当前情绪演出
当前语气
当前自然语言反应
```

---

## Runtime 负责

```text
跨轮心理连续性
背景心境
情绪余波
长期记忆
用户交互模型
未尽之事
主动动力
候选意图
动机决策
协议一致性
```

---

## 主 LLM 不负责

```text
直接写 Runtime 情绪状态
修改长期记忆事实
修改用户模型数值
绕过边界状态机
直接决定是否内源主动
```

---

# 25. 最重要的新设计原则

正式增加：

> **主 LLM 管“现在这一刻怎么活”；Runtime 管“活过以后留下什么”。**

以及：

> **Runtime 不要求当前轮语义完备，只要求长期状态连续。**

以及：

> **即时反应可以比 Runtime 理解更快；持久认知可以比即时演出更慢。**

以及：

> **不理解可以延迟，但原始事件必须保留。**

---

# 26. 对“情绪模块”的重新定义

情绪模块不再负责：

> “告诉主 LLM 当前这句话应该产生什么即时情绪。”

它负责：

> **“哪些事件给角色留下了持续多久、什么方向、什么强度的心理余波。”**

因此：

```text
即时情绪
→ 主 LLM 演出层

持久情绪
→ Runtime 情绪层
```

这两个概念必须分开。

---

# 27. 对“情绪解释器”的重新定义

情绪解释器不再是实时必需模块。

它变成：

> **持久心理状态的低频语义压缩器。**

没有它：

```text
用模板
```

有它：

```text
更新心理解释缓存
```

因此：

> 情绪解释器可选，情绪状态本身不可选。

---

# 28. 对“关键路径预算”的最终理解

关键路径只包含：

```text
lazy_tick
原始事件写入
高置信硬边界规则
机械性工作局势更新
读取 Runtime 缓存
Prompt 构造
主 LLM 调用
```

不包含：

```text
复杂情绪分析
心理语言化
候选意图生成
深层记忆整理
用户模型总结
后验重解释
```

因为这些都属于：

> **未来状态质量增强，而不是当前回复成立的前提。**

---

# 29. 对弱 VPS 的最终意义

移除本地 2B 后，弱 VPS 不再需要常驻：

```text
1GB+ 生成模型权重
llama.cpp 推理进程
本地模型 warmup
推理任务队列
2B 微调与量化版本维护
```

核心常驻可以只剩：

```text
Bot Runtime
PostgreSQL
轻量任务队列
RAG / 文本检索
可选轻量 embedding
```

这更符合：

> **低成本、长期稳定、可维护**

的项目目标。

---

# 30. 旧流程与新流程对比

## 旧流程

```text
用户消息
↓
语义模型理解
↓
情绪计算
↓
心理解释
↓
主 LLM
↓
回复
```

问题：

```text
重复理解
前置阻塞
本地 CPU 成本高
当前轮过度依赖 Runtime
```

---

## 新流程

```text
用户消息
↓
主 LLM 直接即时理解并演出
↓
回复用户

与此同时 / 之后：

交互沉淀
↓
Runtime 结算长期影响
↓
必要时后验深层理解
↓
影响未来
```

优势：

```text
响应快
职责清楚
资源低
允许延迟理解
长期人格连续性更自然
```

---

# 31. 典型例子：用户说“算了，也没什么”

## 当前轮

主 LLM 直接看到：

```text
完整上下文
+
“算了，也没什么。”
```

它可以：

```text
察觉
没察觉
半察觉
```

这本身就是角色演出。

---

## Runtime

先记录：

```text
raw_event
```

如果无法高置信判断：

```text
semantic_status = unresolved
```

不瞎猜。

---

## 后来

用户说：

```text
“你果然当时没发现。”
```

深层刷新重新解释：

```text
旧事件可能存在失望
```

生成：

```text
reappraisal_event
```

现在产生：

```text
懊恼
修复冲动
关系修复候选
```

这形成真正的长期叙事连续性。

---

# 32. 最终架构哲学修正

原先容易把 Runtime 理解成：

> “一个在主 LLM 前面先把人类心理算明白的认知引擎。”

现在应改为：

> **“一个负责保存、结算和演化长期内部状态的持久认知系统。”**

主 LLM：

> 擅长当场理解和表演。

Runtime：

> 擅长让当场发生的事情不会在下一轮消失。

两者互补，而不是重复。

---

# 33. 最终结论

本补丁正式废弃：

```text
本地 2B 作为标准情绪评价 / 情绪解释核心组件
```

并正式确立：

> **即时演出与持久认知分离。**

新的核心结构是：

```text
主 LLM：
负责当前轮的即时理解与表现

Runtime：
负责跨时间的状态连续性

强语义 Provider：
低频负责“后来想明白”

规则 / 统计 / RAG：
负责日常低成本结算
```

最终原则：

> **主 LLM 管“现在这一刻怎么活”；Runtime 管“活过以后留下什么”。**

> **当前轮可以比 Runtime 理解得更快，Runtime 可以比当前轮理解得更深。**

> **没有必要每一秒都完全知道自己为什么有感觉，但绝不能把发生过的事情忘掉。**
